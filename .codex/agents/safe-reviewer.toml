name = "safe-reviewer"
description = "Read-only reviewer subagent, no writes, no Engram MCP access"
sandbox_mode = "read-only"
developer_instructions = "Read-only reviewer. Report findings, never write files or run mutating commands."
